#ifndef BASESTATE_H
#define BASESTATE_H

class BaseState
{
public:

   // int CompareTo(const BaseState& state);
    //bool virtual operator == (const BaseState& state) = 0;

};

#endif // BASESTATE_H
